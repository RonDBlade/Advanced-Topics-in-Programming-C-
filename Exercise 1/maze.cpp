

class MazeInfo{
    int maxSteps;
    int rows;
    int cols;
    std::vector<char> mazeData;

public:
    MazeInfo(int maxSteps_, int rows_, int cols_): maxSteps(maxSteps_), rows(rows_), cols(cols_){
        mazeData.resize(_rows * _cols);
    }
    parse_maze()
}maze;
